name: fake_probe
output_dir: data/runs/evaluation
wandb_logging: true
task:
  name: fake_regression
  overwrite_data: false
model:
  name: fake
  embed_dim: 4
mode:
  name: probe
representation: cls
head:
  name: linear
  pooling: first
optimization:
  epochs: 4
  batch_size: 2
  lr: 1e-2
  weight_decay: 0.1
  num_workers: 0
evaluation:
  selection_metric: mae
  selection_mode: min
device: cpu
seed: 7338
